# City Clerk -- Actions

## 1.3.1
Ran `downstream` and `dispatch` for thread 1 (`1.1.1`-`1.2.2`): Building
Inspector's escalation, Mayor's authorization, Building Inspector's
execution, and City Planner's pending retroactive permit. Recorded routing
in `world/dispatch.json`. Worth watching, per `provenance-audit`, whether
"confirm systemic, brace it, retroactively permit it" becomes a repeatable
pattern rather than a one-off.

## 2.2.1
Ran `downstream` and `dispatch` for thread 2 (`2.1.1`-`2.1.3`): Public Works
noticing the issue, Building Inspector classifying and logging it, Public
Works executing the reconciliation it was assigned. Recorded routing in
`world/dispatch.json`. Clean example of a change that never needed to
leave Public Works's own authority.
