# City Clerk -- Downstream Effects

City Clerk reads every agent's `actions.md` directly while running
`dispatch`, rather than needing a filtered secondhand copy the way other
agents do. This file stays reserved for anything the Clerk would only learn
indirectly -- nothing yet.
