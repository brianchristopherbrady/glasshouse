# Mayor -- Downstream Effects

## 1.1.1
Building Inspector classified `issue-overpass-load` as `systemic_confirmed`
and is requesting authorization for `correction-brace-overpass` -- a
Mayoral decision is required before Building Inspector may proceed
(`WARN_SYSTEMIC_CASCADE`). Assigned to City Council per `issue-overpass-load`'s
`assignedTo` field.

## 1.1.3
Building Inspector executed `correction-brace-overpass` under the granted
authorization; `world-validation` came back clear on `ERR_*`.

## 1.2.1
City Planner drafted a retroactive permit proposal for the Overpass, pending
Mayoral review.

## 2.1.3
Public Works authorized and executed `correction-formalize-pumphouse-fountain-link`
on its own authority. Informational only -- within Public Works's own
`correctionPermissions`, no Mayoral action required.
