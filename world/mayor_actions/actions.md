# Mayor -- Actions

## 1.1.2
Reviewed Building Inspector's escalation on `anomaly-overpass-load`
(`systemic_confirmed`, `1.1.1`). Granted `auth-council-2024-overpass-brace`,
authorizing `reinforce` against `rel-overpass-supports-transit` rather than
`sever` -- severing during business hours would strand Transit Hub Terminal
Three's east entrance with no documented fallback route. Recorded protected
invariant: the east entrance must remain passable during business hours.
Handed off to Building Inspector to execute.

## 1.2.2
Received City Planner's draft retroactive permit proposal for the Overpass
(`1.2.1`). Holding it pending confirmation that `correction-brace-overpass`
actually holds under real load -- authorizing a permanent permit before
that's confirmed would be premature (Municipal Code Section 2: small,
reversible work first). Not yet authorized.
