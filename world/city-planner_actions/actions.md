# City Planner -- Actions

## 1.2.1
Drafted a retroactive permit relationship for the Overpass
(`the-overpass` -> `city-council`, proposed id `rel-overpass-permit-pending`,
kind `permit-of-record`), now that Building Inspector has confirmed
(`1.1.1`) it's genuinely load-bearing. Framed as closing `the-overpass`'s
open provenance after the fact -- see `provenance-audit`. Not yet added to
`world/relationships.json`; submitted to Mayor for authorization once
`correction-brace-overpass` is confirmed holding.
