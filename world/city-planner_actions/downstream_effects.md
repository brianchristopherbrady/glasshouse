# City Planner -- Downstream Effects

## 1.1.1
Building Inspector confirmed `issue-overpass-load` is `systemic_confirmed`
-- the Overpass's dependency on Transit Hub Terminal Three's east entrance
is real and load-bearing, which is why a retroactive permit is worth
drafting.

## 1.1.3
`change-brace-overpass` executed and validated. Worth waiting for
confirmation it holds before pushing the retroactive permit forward.

## 1.2.2
Mayor is holding the retroactive permit proposal pending confirmation the
reinforcement holds -- no further action needed here until that resolves.
