# Public Works -- Downstream Effects

## 1.1.1
Building Inspector classified `anomaly-overpass-load` as `systemic_confirmed`
-- structural, not infrastructure, so no action needed from Public Works.

## 1.1.3
`correction-brace-overpass` executed under Mayoral authorization -- outside
Public Works's domain, informational only.

## 2.1.2
Building Inspector confirmed the water-pressure anomaly is
infrastructure-scoped and handed it back -- clear to authorize and execute
the reconciliation directly.
