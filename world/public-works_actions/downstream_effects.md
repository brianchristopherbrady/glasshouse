# Public Works -- Downstream Effects

## 1.1.1
Building Inspector classified `issue-overpass-load` as `systemic_confirmed`
-- structural, not infrastructure, so no action needed from Public Works.

## 1.1.3
`correction-brace-overpass` executed under Mayoral authorization -- outside
Public Works's domain, informational only.

## 2.1.2
Building Inspector confirmed the water-pressure issue is
infrastructure-scoped and assigned it to Public Works -- clear to authorize
and execute the reconciliation directly.
