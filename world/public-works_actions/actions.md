# Public Works -- Actions

## 2.1.1
Noticed East Canal Pumphouse's pressure dropping every time Founders' Plaza
Fountain runs its evening cycle -- traced the correlation to
`rel-pumphouse-feeds-fountain`, previously `provenance: organic` and
undocumented as a formal dependency. Flagged to Building Inspector for
formal classification rather than quietly patching the pressure schedule
(see `incident-classification`).

## 2.1.3
Building Inspector confirmed (`2.1.2`) this is infrastructure-scoped
(`systemic_candidate`). Authorized `correction-formalize-pumphouse-fountain-link`
(`reconcile`) under `auth-dpw-2024-pumphouse-fountain-link` -- within Public
Works's own `correctionPermissions`, no Mayoral sign-off required. Executed;
ran `world-validation`, `ERR_*` clear.
