# Chapter 4: The Widget Manifest

Placeholder chapter text. In-universe counterpart to the widget status
lifecycle (active -> archived) implemented in `mini-monorepo`'s
`@mini/core` and mirrored in `mini-app`'s `api`/`workers`: a fictional
signal ledger whose entries move from active to archived once the nightly
watch confirms they're no longer transmitting. Declared as depending on
Chapter 3 in `flowbook.members.json`.
